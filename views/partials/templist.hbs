<div class="temp-list">
	<ul>
		{{#each card}}
			<li>
				<a href="/edituser/{{_id}}">
					<img src="/coverImg/{{coverFront}}" alt="">
					<p>{{title}}</p>
				</a>
				<i class="fa fa-heart collection"></i>
			</li>
		{{/each}}
	</ul>
</div>	